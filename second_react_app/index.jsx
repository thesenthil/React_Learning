// what is react fiber:= React Fiber is the core rendering and reconciliation engine introduced in React 16 to radically improve UI responsiveness and performance. It is a complete rewrite of React's internal reconciliation algorithm.Instead of treating the user interface as a single, blocking block of execution, Fiber breaks rendering down into small, prioritized units of work. This architecture acts as the foundational engine powering modern concurrent features like Suspense and Transitions.
// React Fiber is the new rendering engine of React (introduced in React 16). Its job is to decide how and when the UI should update.Instead of updating everything at once, Fiber breaks work into smaller pieces.

// reconsilation=Reconciliation is the process React uses to find what changed in the UI.Whenever state or props change, React asks the Fiber engine to compare the new virtual DOM with the previous one and determine what has changed. It then updates only the parts of the UI that need to be updated, rather than re-rendering the entire UI. This makes React applications more efficient and responsive. The reconciliation process is optimized to minimize the number of DOM operations, which can be expensive in terms of performance. React uses a diffing algorithm to efficiently update the DOM by comparing the new virtual DOM with the previous one and applying only the necessary changes.

//diffing algorithm=The diffing algorithm is a key part of React's reconciliation process. It compares the new virtual DOM with the previous one to determine what has changed. The algorithm is optimized to minimize the number of DOM operations, which can be expensive in terms of performance. It uses heuristics to efficiently update the DOM by applying only the necessary changes, rather than re-rendering the entire UI. This makes React applications more efficient and responsive.